Front-end TODOs (short checklist)

- [ ] Add Tailwind config and install dependencies
- [ ] Implement UI components for Book detail, Search, Chat (AI assistant)
- [ ] Wire `VITE_API_URL` in `.env` and update `src/services/api.ts` baseURL
- [ ] Add Authentication (JWT/session) if desired
- [ ] Replace mock data with live data from backend
